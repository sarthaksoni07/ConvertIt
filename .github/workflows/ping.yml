name: Ping Website

on:
  schedule:
    # Runs every 30 minutes
    - cron: "*/25 * * * *"
  workflow_dispatch:  # Allows manual run from GitHub UI

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping website
        run: |
          curl -I https://convert-itt.vercel.app
